{
  "name": "erp-agent-cdk",
  "version": "2.0.0",
  "bin": {
    "erp-agent-cdk": "bin/app.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "22.7.9",
    "aws-cdk": "^2.1133.0",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.12",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "//overrides": "@babel/core reaches us only transitively via jest, whose range still resolves to a vulnerable 7.29.0; no direct dependent can be bumped to fix it. brace-expansion arrived at three majors (1.x via test-exclude, 2.x, 5.x); 5.0.9 is a deliberate cross-major force on the 1.x/2.x consumers — verified against jest and cdk synth. It cannot reach aws-cdk-lib's copy, which is bundled inside its tarball; that one only moves with aws-cdk-lib itself, and 2.265.0 is the first release carrying 5.0.9.",
  "overrides": {
    "@babel/core": "^7.29.7",
    "brace-expansion": "^5.0.9"
  },
  "dependencies": {
    "@aws-cdk/aws-amplify-alpha": "^2.215.0-alpha.0",
    "@aws-cdk/aws-bedrock-agentcore-alpha": "^2.233.0-alpha.0",
    "@aws-cdk/aws-bedrock-alpha": "^2.233.0-alpha.0",
    "@aws-sdk/client-bedrock-agentcore": "^3.1096.0",
    "@aws-sdk/client-cloudformation": "^3.1096.0",
    "@aws-sdk/client-secrets-manager": "^3.1096.0",
    "aws-cdk-lib": "^2.262.1",
    "constructs": "^10.7.1",
    "yaml": "^2.9.0"
  }
}
