# Exclude internal-only content from `git archive` exports
# These paths are stripped when running: git archive --format=tar HEAD

# Internal documentation
.docs-internal/ export-ignore

# Amazon security scanner config
.ash.yaml export-ignore

# Security scanner output + configs (Amazon-internal tooling)
.ash/ export-ignore
.checkov.yaml export-ignore
.kics.yml export-ignore
.secrets.baseline export-ignore

# Internal auth testing handoff (WIP status, tester assignments, phase roadmap — not adopter docs)
docs/auth-testing-handoff.md export-ignore

# GitLab CI (internal) — published sample uses GitHub Actions
.gitlab-ci.yml export-ignore

# Local task/backlog notes (not version-controlled)
.backlog/ export-ignore

# Internal isolation repros + SAP-auth smoke test (personal email, internal tenant/env
# identifiers, internal auth-debugging context — not adopter docs)
test-scripts/repro-agentcore-3lo.py export-ignore
test-scripts/repro-agentcore-obo.py export-ignore
test-scripts/test-okta-sap-local.py export-ignore
test-scripts/.env.example export-ignore
